{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "preinstall": "sh -c 'rm -f package-lock.json yarn.lock; case \"$npm_config_user_agent\" in pnpm/*) ;; *) echo \"Use pnpm instead\" >&2; exit 1 ;; esac'",
    "build": "pnpm run typecheck && pnpm -r --if-present run build",
    "build:deploy": "pnpm --filter @workspace/starfield run build && pnpm --filter @workspace/api-server run build",
    "typecheck:libs": "tsc --build",
    "typecheck": "pnpm run typecheck:libs && pnpm -r --filter \"./artifacts/**\" --filter \"./scripts\" --if-present run typecheck",
    "dev:api": "pnpm --filter @workspace/api-server run dev",
    "dev:api:watch": "pnpm --filter @workspace/api-server run dev:watch",
    "dev:web": "pnpm --filter @workspace/starfield run dev",
    "dev": "echo 'Run in two terminals:\\n  pnpm dev:api:watch\\n  pnpm dev:web' && exit 1",
    "db:up": "docker compose up -d postgres",
    "db:down": "docker compose down",
    "db:studio": "pnpm --filter @workspace/db exec drizzle-kit studio --config ./drizzle.config.ts",
    "db:push": "pnpm --filter @workspace/db run push",
    "db:push:force": "pnpm --filter @workspace/db run push-force",
    "codegen": "pnpm --filter @workspace/api-spec run codegen"
  },
  "private": true,
  "devDependencies": {
    "prettier": "^3.8.3",
    "typescript": "~5.9.3"
  }
}
